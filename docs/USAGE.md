# Usage

- `node src/cli.js demo` — show the control plane output
- `node src/cli.js compact` — compact stdin
- `node src/cli.js plan` — predict next tools from stdin
- `node src/benchmark/benchmark.js` — run benchmark suite
- `node src/benchmark/dashboard.js` — generate dashboard report
